// PACKAGE_PATH
const MAIN = '/pages'

/* eslint-disable */
export default {
  main: {
    PACKAGE: MAIN,
    INDEX: `${MAIN}/index`,
    MINE: `${MAIN}/mine`,
    LOGIN:`${MAIN}/login`,
    ORDER_LIST:`${MAIN}/order-list`,

    // main
  }
}
